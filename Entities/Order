{
  "name": "Order",
  "type": "object",
  "properties": {
    "group_id": {
      "type": "string",
      "description": "ID of the group this order belongs to"
    },
    "title": {
      "type": "string",
      "description": "Order title/name"
    },
    "deadline": {
      "type": "string",
      "format": "date-time",
      "description": "Deadline to add cards"
    },
    "status": {
      "type": "string",
      "enum": [
        "open",
        "closed",
        "ordered",
        "delivered"
      ],
      "default": "open"
    },
    "total_value": {
      "type": "number",
      "description": "Total order value"
    },
    "currency": {
      "type": "string",
      "default": "USD"
    },
    "notes": {
      "type": "string",
      "description": "Additional notes from the person ordering"
    }
  },
  "required": [
    "group_id",
    "title",
    "deadline"
  ]
}